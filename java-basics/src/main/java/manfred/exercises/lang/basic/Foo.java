package manfred.exercises.lang.basic;

/**
 * 简单枚举示例，用于配合 ClassInitOrderDemo 等练习类做辅助演示。
 *
 * 仅定义三个枚举常量 A、B、C，作为最小化枚举定义的参考样本，
 * 可在测试或演示中作为枚举类型参数传入，验证 switch/EnumSet 等枚举相关 API 的基本用法。
 */
public enum Foo {
    A,
    B,
    C
}
